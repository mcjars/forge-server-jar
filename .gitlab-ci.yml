stages:
  - build

build:
  stage: build
  needs: []
  tags:
    - panel_tests
  image: maven:3-eclipse-temurin-8
  before_script:
    - mvn clean
  script:
    - mvn install
    - java -jar target/ForgeServerJAR.jar
    - /root/minio-binaries/mc cp target/ForgeServerJAR.jar sparked-modpacks
